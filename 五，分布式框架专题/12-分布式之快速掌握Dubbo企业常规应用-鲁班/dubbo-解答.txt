1、（G98）我在项目中做的时候  使用dubbo 做的微服务  apigetway 调用子服务 俩个服务的  service 包的全路径也必须一样是为啥？

2、（E35）dubbo如果遇到循环调用怎么办？
比如服务端和客户端都有互相暴露的服务


3、（H36）
dubbo 如果一个接口有多个实现类，客户端怎么调用不同service实现类。

4、（D44）
   我看到provider中配置的 dubbo:service中的类是服务的接口，但是在 consumer中那个接口还是服务端的一个接口所以客户端和服务端引用的接口是同一个，疑问，项目部署的时候client端肯定不能调用到service端的接口的。
   那么如果我服务端和客户端不在一个机器上，难道我需要把service的代码打成jar包给客户端使用？
